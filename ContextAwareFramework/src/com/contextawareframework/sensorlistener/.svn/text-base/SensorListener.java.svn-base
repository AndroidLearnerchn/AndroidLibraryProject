package com.contextawareframework.sensorlistener;

import android.hardware.SensorEventListener;

/****************************************************************************
 * This interface is the Listener Available sensor which has been registered 
 * to the OS. If user wants to work directly on sensor data without storing 
 * it in the database then can implement this interface. 
 * **************************************************************************/
//Abandoned
public interface SensorListener extends SensorEventListener{

}
